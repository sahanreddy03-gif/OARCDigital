# Environment variables reference — OARC Digital (Next.js cutover)

Generated: 2026-04-29
Total unique vars: 25

Legend:
- Phase: BUILD = required for `next build` to succeed; RUNTIME = only needed when feature runs;
  BUILD-INLINE = NEXT_PUBLIC_* baked into client bundle, app builds & runs without it.
- Visibility: SECRET = server-only; PUBLIC = NEXT_PUBLIC_* exposed to browser.
- Cutover-blocker: YES = build cannot pass without it; NO = safe to defer.

---

## Build blockers

### DATABASE_URL
- Used by: lib/db.ts, lib/storage.ts, drizzle.config.ts. Consumed by /api/leads (POST + GET).
- Phase: RUNTIME after this commit's fix. Was BUILD before — lib/db.ts threw at module load.
- Visibility: SECRET.
- Placeholder: postgres://user:pass@ep-xxx.eu-central-1.aws.neon.tech/db?sslmode=require
- Cutover-blocker: NO (after fix). YES historically.
- Disable acceptable for cutover: YES — Formspree fallback still captures leads to email.

## Runtime-only secrets (build passes without them; specific endpoints 503 until set)

### LEADS_ADMIN_TOKEN
- Used by: app/api/leads/route.ts (GET handler). Already guarded — returns 503 if unset.
- Visibility: SECRET. Placeholder: `openssl rand -hex 32`.
- Disable acceptable for cutover: YES.

### LEAD_WEBHOOK_URL
- Used by: api/leads.ts (LEGACY Vite-era — verify if still mounted; safe to delete).
- Visibility: SECRET. Disable acceptable: YES.

### XAI_API_KEY
- Used by: app/api/chat/route.ts. Already guarded — getGrokClient() returns null if unset.
- Visibility: SECRET. Disable acceptable: PARTIAL (chat falls back to instant-response/guided-flow).

### CLOUDFLARE_ACCOUNT_ID / CLOUDFLARE_AI_MODEL / CLOUDFLARE_AI_TOKEN
- Used by: app/api/chat/route.ts (Cloudflare Workers AI fallback when xAI fails).
- Visibility: SECRET. Disable acceptable: YES.

### INDEXNOW_KEY
- Used by: lib/indexNow.ts, scripts/index-now-ping.ts (post-build URL ping to Bing/Yandex).
- Visibility: SECRET (the matching public/<KEY>.txt file is intentionally public).
- Disable acceptable: YES.

## Public (NEXT_PUBLIC_*) — baked into client bundle, no crash if missing

All five degrade silently at runtime if undefined.

- NEXT_PUBLIC_GA4_ID — components/Analytics.tsx (GA4 tracking)
- NEXT_PUBLIC_CLARITY_ID — components/Analytics.tsx (Microsoft Clarity)
- NEXT_PUBLIC_CALCOM_BOOKING_URL — lib/utm.ts (booking links)
- NEXT_PUBLIC_FOUNDER_LINKEDIN / NEXT_PUBLIC_FOUNDER_TWITTER — lib/schema/index.ts (Person JSON-LD)

## Auto-set by Vercel (DO NOT configure manually)

- NODE_ENV (production on Vercel, development locally)
- VERCEL_GIT_PREVIOUS_SHA (used by index-now-ping for delta computation)

## Test/CI-only (Vercel never reads these)

- BASE, PLAYWRIGHT_BASE_URL, PLAYWRIGHT_CHROMIUM_PATH
- LIGHTHOUSE_CHROMIUM_PATH, LIGHTHOUSE_ROUTE_FILTER, LIGHTHOUSE_RUNS_PER_ROUTE
- AUDIT_FULL, AUDIT_SAMPLE, AUDIT_URLS, AUDIT_CONCURRENCY

---

## Summary — what to set in Vercel dashboard for cutover

Required to unblock build: NONE (after this commit's fix).

Strongly recommended (clean degradation if absent):
- DATABASE_URL — re-enables lead persistence
- LEADS_ADMIN_TOKEN — admin lead-list endpoint
- XAI_API_KEY — smart chat replies
- NEXT_PUBLIC_GA4_ID, NEXT_PUBLIC_CLARITY_ID — analytics
- INDEXNOW_KEY (+ matching public/<KEY>.txt) — speed up indexing of new URLs

Optional fallback:
- Cloudflare AI trio
- Cal.com URL, founder socials----------------i ned all theze details to give replit