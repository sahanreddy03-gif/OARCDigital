STOP PUSHING FIXES. Vercel deploys keep failing on the 
same root cause: missing environment variables.

Latest error from Vercel build log:
"Error: DATABASE_URL is not set"
"Failed to collect page data for /api/leads"

Each failed push wastes 2-3 minutes and clutters deployment 
history. Don't push another commit until env vars are 
sorted.

ACTION ITEM 1 — List all required env vars

In Replit Shell, run:

grep -r "process.env" --include="*.ts" --include="*.tsx" \
  --include="*.js" -h | grep -oP "process\.env\.\w+" | \
  sort -u > .local/required-env-vars.txt

cat .local/required-env-vars.txt

Paste the output. I need the complete list.

ACTION ITEM 2 — For each env var, document

Per env var, document in .local/env-var-reference.md:
- Variable name
- What feature uses it (lookup in code)
- Whether it's required for build OR only at runtime
- Whether it's public (NEXT_PUBLIC_*) or secret
- Recommended placeholder value if not yet configured
- Whether disabling that feature is acceptable for cutover

ACTION ITEM 3 — Make build resilient to missing env vars

For env vars that are only needed at runtime (like DATABASE_URL 
for /api/leads), the code should NOT crash the build when 
they're missing. Modify /api/leads/route.ts to handle missing 
DATABASE_URL gracefully:

- If DATABASE_URL exists: connect to database
- If DATABASE_URL is missing: return 503 "service unavailable" 
  at runtime, but don't crash build

Pattern:
const url = process.env.DATABASE_URL;
if (!url) {
  return Response.json(
    { error: "Database not configured" }, 
    { status: 503 }
  );
}
// continue with database connection

This makes the build pass even when env vars are missing. 
Production routes that need env vars return 503 until vars 
are configured. Other 620 pages still ship.

ACTION ITEM 4 — Don't push fixes until I confirm

After Action Items 1-3 complete locally, paste:
- The required-env-vars list
- The env-var-reference.md content
- The diff showing /api/leads modification

Wait for my "PROCEED — PUSH FIX" before pushing.

I'll handle Vercel env var configuration in parallel via 
the Vercel dashboard. We'll merge changes when both sides 
ready.