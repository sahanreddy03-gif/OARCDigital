# 🚀 ENHANCED SPLIT SCREEN - COMPLETE BUILD SPECIFICATION

Let’s build the perfect balance. Here’s everything Replit needs.

-----

## 📋 COMPLETE REPLIT PROMPTS

-----

### 🎯 **PROMPT 1: FOUNDATION & LAYOUT**

```
BUILD ENHANCED SPLIT SCREEN HERO - FOUNDATION:

═══════════════════════════════════════════════════════════
OVERALL STRUCTURE
═══════════════════════════════════════════════════════════

Container:
.hero-split-screen {
  width: 100vw;
  height: 100vh;
  display: flex;
  position: relative;
  overflow: hidden;
  background: linear-gradient(
    135deg,
    #FDFCFA 0%,
    #F8F7F4 50%,
    #FDFCFA 100%
  );
}

Three Main Sections:
1. Left Side (40% width): Creative showcase
2. Center (20% width): Fusion zone with content
3. Right Side (40% width): AI systems

═══════════════════════════════════════════════════════════
BACKGROUND LAYERS (Subtle Parallax)
═══════════════════════════════════════════════════════════

Create 3 background layers for depth:

Layer 1 (Furthest - slowest):
.bg-layer-1 {
  position: absolute;
  inset: 0;
  background: radial-gradient(
    circle at 20% 50%,
    rgba(255,107,157,0.03),
    transparent 50%
  );
  transition: transform 0.5s ease-out;
}

Layer 2 (Middle):
.bg-layer-2 {
  position: absolute;
  inset: 0;
  background: radial-gradient(
    circle at 80% 50%,
    rgba(0,255,65,0.03),
    transparent 50%
  );
  transition: transform 0.3s ease-out;
}

Layer 3 (Closest - fastest):
.bg-layer-3 {
  position: absolute;
  inset: 0;
  background: radial-gradient(
    circle at 50% 50%,
    rgba(139,92,246,0.04),
    transparent 70%
  );
  transition: transform 0.2s ease-out;
}

Parallax on Mouse Move:
document.addEventListener('mousemove', (e) => {
  const mouseX = (e.clientX / window.innerWidth - 0.5) * 2;
  const mouseY = (e.clientY / window.innerHeight - 0.5) * 2;
  
  // Layer 1 moves slowest
  bgLayer1.style.transform = `translate(${mouseX * 20}px, ${mouseY * 20}px)`;
  
  // Layer 2 medium speed
  bgLayer2.style.transform = `translate(${mouseX * 40}px, ${mouseY * 40}px)`;
  
  // Layer 3 fastest
  bgLayer3.style.transform = `translate(${mouseX * 60}px, ${mouseY * 60}px)`;
});

═══════════════════════════════════════════════════════════
LEFT SIDE: CREATIVE SHOWCASE
═══════════════════════════════════════════════════════════

Container:
.creative-side {
  width: 40%;
  height: 100%;
  position: relative;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 80px 40px;
}

Work Cards Grid (6-8 pieces):
.work-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 24px;
  max-width: 600px;
}

Individual Card:
.work-card {
  background: white;
  border-radius: 16px;
  overflow: hidden;
  box-shadow: 
    0 8px 32px rgba(0,0,0,0.08),
    0 2px 8px rgba(0,0,0,0.04);
  transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
  cursor: pointer;
  position: relative;
}

/* Vary card sizes */
.work-card:nth-child(1) {
  grid-row: span 1;
  aspect-ratio: 1/1;
}

.work-card:nth-child(2) {
  grid-row: span 2;
  aspect-ratio: 1/1.3;
}

.work-card:nth-child(3) {
  grid-row: span 1;
  aspect-ratio: 1/1;
}

.work-card:nth-child(4) {
  grid-row: span 1;
  aspect-ratio: 1/1;
}

.work-card:nth-child(5) {
  grid-column: span 2;
  aspect-ratio: 2/1;
}

.work-card:nth-child(6) {
  grid-row: span 1;
  aspect-ratio: 1/1;
}

Card Content:
.work-card img,
.work-card video {
  width: 100%;
  height: 100%;
  object-fit: cover;
}

.work-card video {
  autoplay: true;
  loop: true;
  muted: true;
  playsinline: true;
}

Floating Animation:
@keyframes gentle-float {
  0%, 100% {
    transform: translateY(0px);
  }
  50% {
    transform: translateY(-12px);
  }
}

.work-card {
  animation: gentle-float 4s ease-in-out infinite;
}

/* Stagger animation */
.work-card:nth-child(1) { animation-delay: 0s; }
.work-card:nth-child(2) { animation-delay: 0.5s; }
.work-card:nth-child(3) { animation-delay: 1s; }
.work-card:nth-child(4) { animation-delay: 1.5s; }
.work-card:nth-child(5) { animation-delay: 2s; }
.work-card:nth-child(6) { animation-delay: 2.5s; }

Hover State:
.work-card:hover {
  transform: translateY(-16px) scale(1.05);
  box-shadow: 
    0 24px 60px rgba(255,107,157,0.15),
    0 8px 20px rgba(0,0,0,0.08);
  z-index: 10;
}

Card Overlay (on hover):
.work-card::after {
  content: '';
  position: absolute;
  inset: 0;
  background: linear-gradient(
    to top,
    rgba(0,0,0,0.7),
    transparent
  );
  opacity: 0;
  transition: opacity 0.3s;
}

.work-card:hover::after {
  opacity: 1;
}

.work-info {
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;
  padding: 20px;
  color: white;
  opacity: 0;
  transform: translateY(20px);
  transition: all 0.3s;
  z-index: 2;
}

.work-card:hover .work-info {
  opacity: 1;
  transform: translateY(0);
}

.work-info .platform {
  font-size: 12px;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 1px;
  margin-bottom: 4px;
}

.work-info .metric {
  font-size: 18px;
  font-weight: 700;
}

═══════════════════════════════════════════════════════════
CENTER: FUSION ZONE
═══════════════════════════════════════════════════════════

Container:
.fusion-center {
  width: 20%;
  height: 100%;
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 60px 20px;
  z-index: 100;
}

Energy Line (Vertical):
.energy-line {
  position: absolute;
  left: 50%;
  top: 0;
  bottom: 0;
  width: 2px;
  transform: translateX(-50%);
  background: linear-gradient(
    to bottom,
    rgba(255,107,157,0.3) 0%,
    rgba(139,92,246,0.5) 50%,
    rgba(0,255,65,0.3) 100%
  );
  box-shadow: 0 0 20px rgba(139,92,246,0.4);
  animation: energy-pulse 3s ease-in-out infinite;
}

@keyframes energy-pulse {
  0%, 100% {
    opacity: 0.6;
    box-shadow: 0 0 20px rgba(139,92,246,0.4);
  }
  50% {
    opacity: 1;
    box-shadow: 0 0 40px rgba(139,92,246,0.6);
  }
}

Logo (Top 30%):
.fusion-logo {
  width: 100px;
  margin-bottom: 48px;
  filter: drop-shadow(0 4px 20px rgba(0,0,0,0.1));
  animation: logo-float 4s ease-in-out infinite;
  z-index: 2;
}

@keyframes logo-float {
  0%, 100% {
    transform: translateY(0px);
  }
  50% {
    transform: translateY(-8px);
  }
}

Headline:
.split-headline {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 16px;
  margin-bottom: 32px;
  z-index: 2;
}

.headline-part {
  font-size: clamp(32px, 4vw, 56px);
  font-weight: 900;
  text-align: center;
  line-height: 1.1;
}

.creative-text {
  background: linear-gradient(135deg, #FF6B9D, #C77DFF);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  display: block;
}

.fusion-symbol {
  font-size: clamp(40px, 5vw, 64px);
  color: #8B5CF6;
  margin: 8px 0;
  animation: symbol-rotate 8s linear infinite;
}

@keyframes symbol-rotate {
  from { transform: rotate(0deg); }
  to { transform: rotate(360deg); }
}

.ai-text {
  background: linear-gradient(135deg, #00FF41, #00D4FF);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  display: block;
}

Subheadline:
.subheadline {
  font-size: clamp(16px, 1.8vw, 22px);
  color: #525252;
  text-align: center;
  max-width: 400px;
  line-height: 1.6;
  margin-bottom: 24px;
  z-index: 2;
}

Proof Metric:
.proof-metric {
  font-size: clamp(14px, 1.5vw, 18px);
  color: #6B6B6B;
  text-align: center;
  max-width: 350px;
  line-height: 1.5;
  margin-bottom: 40px;
  z-index: 2;
}

.proof-metric strong {
  color: #FF6B35;
  font-weight: 700;
}

CTA Button:
.fusion-cta {
  padding: 18px 48px;
  font-size: 18px;
  font-weight: 700;
  background: linear-gradient(135deg, #FF6B35, #C77DFF);
  color: white;
  border: none;
  border-radius: 12px;
  cursor: pointer;
  box-shadow: 
    0 8px 24px rgba(255,107,53,0.25),
    0 2px 8px rgba(0,0,0,0.08);
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  position: relative;
  overflow: hidden;
  z-index: 2;
}

.fusion-cta::before {
  content: '';
  position: absolute;
  top: 50%;
  left: 50%;
  width: 0;
  height: 0;
  border-radius: 50%;
  background: rgba(255,255,255,0.3);
  transform: translate(-50%, -50%);
  transition: width 0.6s, height 0.6s;
}

.fusion-cta:hover::before {
  width: 300px;
  height: 300px;
}

.fusion-cta:hover {
  transform: translateY(-4px);
  box-shadow: 
    0 12px 32px rgba(255,107,53,0.3),
    0 4px 12px rgba(0,0,0,0.1);
}

.fusion-cta span {
  position: relative;
  z-index: 1;
}

═══════════════════════════════════════════════════════════
RIGHT SIDE: AI SYSTEMS
═══════════════════════════════════════════════════════════

Container:
.ai-side {
  width: 40%;
  height: 100%;
  position: relative;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 80px 40px;
}

Visual Elements Container:
.ai-visuals {
  width: 100%;
  max-width: 600px;
  display: flex;
  flex-direction: column;
  gap: 32px;
}

Code Terminal:
.code-terminal {
  background: rgba(13,17,23,0.6);
  border: 1px solid rgba(0,255,65,0.15);
  border-radius: 12px;
  padding: 24px;
  font-family: 'Monaco', 'Courier New', monospace;
  font-size: 13px;
  color: #00FF41;
  backdrop-filter: blur(10px);
  box-shadow: 
    0 8px 32px rgba(0,0,0,0.12),
    0 0 40px rgba(0,255,65,0.08);
  max-height: 200px;
  overflow: hidden;
}

.code-line {
  margin: 4px 0;
  text-shadow: 0 0 10px rgba(0,255,65,0.5);
  white-space: pre;
}

.code-comment {
  color: #6B7280;
}

.code-keyword {
  color: #8B5CF6;
}

.code-string {
  color: #FF6B9D;
}

Typing Animation:
const codeLines = [
  "// AI Optimization Engine",
  "const analyzer = new ContentAnalyzer();",
  "const insights = analyzer.process(creative);",
  "",
  "insights.optimize({",
  "  target: 'revenue',",
  "  improve: '3x ROAS'",
  "});",
  "",
  "// Result: +340% performance"
];

let currentLine = 0;
let currentChar = 0;
const terminalElement = document.querySelector('.code-terminal');

function typeCode() {
  if (currentLine >= codeLines.length) {
    setTimeout(() => {
      terminalElement.innerHTML = '';
      currentLine = 0;
      currentChar = 0;
      typeCode();
    }, 3000);
    return;
  }
  
  const line = codeLines[currentLine];
  
  if (currentChar === 0) {
    const lineElement = document.createElement('div');
    lineElement.className = 'code-line';
    terminalElement.appendChild(lineElement);
  }
  
  const lineElement = terminalElement.lastChild;
  lineElement.textContent = line.substring(0, currentChar + 1);
  
  currentChar++;
  
  if (currentChar > line.length) {
    currentChar = 0;
    currentLine++;
    setTimeout(typeCode, 300);
  } else {
    setTimeout(typeCode, 30);
  }
}

// Start typing on load
setTimeout(typeCode, 1000);

Data Visualization:
.data-viz {
  background: white;
  border-radius: 12px;
  padding: 24px;
  box-shadow: 
    0 8px 32px rgba(0,0,0,0.08),
    0 2px 8px rgba(0,0,0,0.04);
}

.metric-row {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 16px 0;
  border-bottom: 1px solid #F2F1ED;
}

.metric-row:last-child {
  border-bottom: none;
}

.metric-label {
  font-size: 14px;
  color: #6B6B6B;
  font-weight: 500;
}

.metric-value {
  font-size: 24px;
  font-weight: 700;
  color: #1A1A1A;
}

.metric-bar {
  width: 100%;
  height: 6px;
  background: #F2F1ED;
  border-radius: 3px;
  margin-top: 8px;
  overflow: hidden;
}

.metric-fill {
  height: 100%;
  background: linear-gradient(90deg, #00FF41, #00D4FF);
  border-radius: 3px;
  animation: fill-bar 2s ease-out forwards;
}

@keyframes fill-bar {
  from { width: 0%; }
}

.metric-row:nth-child(1) .metric-fill { 
  animation-delay: 0.2s;
  width: 90%; 
}
.metric-row:nth-child(2) .metric-fill { 
  animation-delay: 0.4s;
  width: 75%; 
}
.metric-row:nth-child(3) .metric-fill { 
  animation-delay: 0.6s;
  width: 85%; 
}

Animated Counter:
.metric-value {
  --target: 0;
}

function animateCounter(element, target) {
  let current = 0;
  const increment = target / 60;
  const timer = setInterval(() => {
    current += increment;
    if (current >= target) {
      current = target;
      clearInterval(timer);
    }
    element.textContent = Math.floor(current) + '%';
  }, 16);
}

// Trigger on scroll into view
const observer = new IntersectionObserver((entries) => {
  entries.forEach(entry => {
    if (entry.isIntersecting) {
      const valueElement = entry.target.querySelector('.metric-value');
      const target = parseInt(valueElement.dataset.target);
      animateCounter(valueElement, target);
      observer.unobserve(entry.target);
    }
  });
});

document.querySelectorAll('.metric-row').forEach(row => {
  observer.observe(row);
});

═══════════════════════════════════════════════════════════
MOBILE RESPONSIVE
═══════════════════════════════════════════════════════════

@media (max-width: 1024px) {
  .hero-split-screen {
    flex-direction: column;
    height: auto;
    min-height: 100vh;
  }
  
  .creative-side,
  .ai-side {
    width: 100%;
    min-height: 40vh;
    padding: 60px 24px;
  }
  
  .fusion-center {
    width: 100%;
    padding: 80px 24px;
  }
  
  .work-grid {
    grid-template-columns: repeat(2, 1fr);
    gap: 16px;
  }
  
  .work-card:nth-child(5) {
    grid-column: span 2;
  }
  
  .energy-line {
    width: 100%;
    height: 2px;
    left: 0;
    top: 50%;
    transform: translateY(-50%);
    background: linear-gradient(
      to right,
      rgba(255,107,157,0.3) 0%,
      rgba(139,92,246,0.5) 50%,
      rgba(0,255,65,0.3) 100%
    );
  }
  
  .split-headline {
    flex-direction: column;
  }
  
  .headline-part {
    font-size: clamp(28px, 8vw, 40px);
  }
}
```

-----

### 🎯 **PROMPT 2: PARTICLE SYSTEM**

```
ADD PARTICLE CROSSING SYSTEM - 500 PARTICLES:

═══════════════════════════════════════════════════════════
CANVAS SETUP
═══════════════════════════════════════════════════════════

HTML:
<canvas id="particle-canvas"></canvas>

CSS:
#particle-canvas {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  pointer-events: none;
  z-index: 50;
}

Canvas Initialization:
const canvas = document.getElementById('particle-canvas');
const ctx = canvas.getContext('2d');

function resizeCanvas() {
  canvas.width = window.innerWidth;
  canvas.height = window.innerHeight;
}

resizeCanvas();
window.addEventListener('resize', resizeCanvas);

═══════════════════════════════════════════════════════════
PARTICLE CLASS
═══════════════════════════════════════════════════════════

class Particle {
  constructor(side) {
    this.side = side; // 'creative' or 'ai'
    
    // Starting position based on side
    if (side === 'creative') {
      this.x = Math.random() * (window.innerWidth * 0.4);
      this.y = Math.random() * window.innerHeight;
      this.targetX = window.innerWidth * 0.5;
      this.color = '#FF6B9D';
    } else {
      this.x = window.innerWidth * 0.6 + Math.random() * (window.innerWidth * 0.4);
      this.y = Math.random() * window.innerHeight;
      this.targetX = window.innerWidth * 0.5;
      this.color = '#00FF41';
    }
    
    this.targetY = window.innerHeight * 0.5;
    this.vx = 0;
    this.vy = 0;
    this.size = 2 + Math.random() * 2;
    this.life = 1.0;
    this.maxLife = 1.0;
    this.speed = 0.02 + Math.random() * 0.02;
    this.trail = [];
    this.maxTrail = 10;
  }
  
  update() {
    // Move toward center
    const dx = this.targetX - this.x;
    const dy = this.targetY - this.y;
    
    this.vx += dx * this.speed;
    this.vy += dy * this.speed;
    
    // Apply drag
    this.vx *= 0.95;
    this.vy *= 0.95;
    
    this.x += this.vx;
    this.y += this.vy;
    
    // Update trail
    this.trail.push({ x: this.x, y: this.y });
    if (this.trail.length > this.maxTrail) {
      this.trail.shift();
    }
    
    // Check if reached center
    const distance = Math.sqrt(dx * dx + dy * dy);
    if (distance < 30) {
      this.life -= 0.05;
    }
    
    // Decay life slowly
    this.life -= 0.002;
  }
  
  draw(ctx) {
    if (this.life <= 0) return;
    
    ctx.globalAlpha = this.life;
    
    // Draw trail
    if (this.trail.length > 1) {
      ctx.beginPath();
      ctx.strokeStyle = this.color;
      ctx.lineWidth = this.size * 0.5;
      ctx.shadowBlur = 10;
      ctx.shadowColor = this.color;
      
      for (let i = 0; i < this.trail.length; i++) {
        const point = this.trail[i];
        if (i === 0) {
          ctx.moveTo(point.x, point.y);
        } else {
          ctx.lineTo(point.x, point.y);
        }
      }
      ctx.stroke();
    }
    
    // Draw particle
    ctx.fillStyle = this.color;
    ctx.shadowBlur = 15;
    ctx.shadowColor = this.color;
    ctx.beginPath();
    ctx.arc(this.x, this.y, this.size, 0, Math.PI * 2);
    ctx.fill();
    
    ctx.globalAlpha = 1;
    ctx.shadowBlur = 0;
  }
}

═══════════════════════════════════════════════════════════
PARTICLE MANAGEMENT
═══════════════════════════════════════════════════════════

const particles = [];
const MAX_PARTICLES = 500;
const SPAWN_RATE = 100; // milliseconds

// Spawn particles alternating sides
let spawnFromCreative = true;

setInterval(() => {
  if (particles.length < MAX_PARTICLES) {
    const side = spawnFromCreative ? 'creative' : 'ai';
    particles.push(new Particle(side));
    spawnFromCreative = !spawnFromCreative;
  }
}, SPAWN_RATE);

═══════════════════════════════════════════════════════════
ANIMATION LOOP
═══════════════════════════════════════════════════════════

function animate() {
  // Clear canvas
  ctx.clearRect(0, 0, canvas.width, canvas.height);
  
  // Update and draw particles
  for (let i = particles.length - 1; i >= 0; i--) {
    const particle = particles[i];
    particle.update();
    
    if (particle.life > 0) {
      particle.draw(ctx);
    } else {
      // Remove dead particles
      particles.splice(i, 1);
    }
  }
  
  requestAnimationFrame(animate);
}

// Start animation
animate();

═══════════════════════════════════════════════════════════
PERFORMANCE OPTIMIZATION
═══════════════════════════════════════════════════════════

// Reduce particles on mobile
if (window.innerWidth < 768) {
  MAX_PARTICLES = 200;
  SPAWN_RATE = 150;
}

// Pause when tab not visible
document.addEventListener('visibilitychange', () => {
  if (document.hidden) {
    // Pause spawning
    clearInterval(spawnInterval);
  } else {
    // Resume spawning
    spawnInterval = setInterval(spawnParticle, SPAWN_RATE);
  }
});
```

-----

### 🎯 **PROMPT 3: ENTRANCE ANIMATION**

```
ADD ENTRANCE ANIMATION SEQUENCE:

═══════════════════════════════════════════════════════════
GSAP TIMELINE (Or CSS Animations)
═══════════════════════════════════════════════════════════

Initial State (Hidden):
.hero-split-screen {
  opacity: 0;
}

.work-card {
  opacity: 0;
  transform: translateX(-100px) scale(0.9);
}

.ai-visuals > * {
  opacity: 0;
  transform: translateX(100px) scale(0.9);
}

.fusion-center > * {
  opacity: 0;
  transform: translateY(30px);
}

.energy-line {
  opacity: 0;
  transform: translateX(-50%) scaleY(0);
  transform-origin: top center;
}

Entrance Timeline:
function playEntranceAnimation() {
  // Background fade in
  gsap.to('.hero-split-screen', {
    opacity: 1,
    duration: 0.8,
    ease: 'power2.out'
  });
  
  // Creative cards slide in from left
  gsap.to('.work-card', {
    opacity: 1,
    x: 0,
    scale: 1,
    duration: 1,
    stagger: 0.1,
    ease: 'power3.out',
    delay: 0.3
  });
  
  // AI visuals slide in from right
  gsap.to('.ai-visuals > *', {
    opacity: 1,
    x: 0,
    scale: 1,
    duration: 1,
    stagger: 0.15,
    ease: 'power3.out',
    delay: 0.3
  });
  
  // Energy line grows
  gsap.to('.energy-line', {
    opacity: 1,
    scaleY: 1,
    duration: 1.2,
    ease: 'power2.inOut',
    delay: 0.8
  });
  
  // Logo appears
  gsap.to('.fusion-logo', {
    opacity: 1,
    y: 0,
    duration: 0.8,
    ease: 'back.out(1.5)',
    delay: 1.2
  });
  
  // Headline parts
  gsap.to('.creative-text', {
    opacity: 1,
    y: 0,
    duration: 0.6,
    ease: 'power2.out',
    delay: 1.6
  });
  
  gsap.to('.fusion-symbol', {
    opacity: 1,
    y: 0,
    scale: 1,
    duration: 0.5,
    ease: 'back.out(2)',
    delay: 1.8
  });
  
  gsap.to('.ai-text', {
    opacity: 1,
    y: 0,
    duration: 0.6,
    ease: 'power2.out',
    delay: 2.0
  });
  
  // Subheadline
  gsap.to('.subheadline', {
    opacity: 1,
    y: 0,
    duration: 0.6,
    delay: 2.3
  });
  
  // Proof metric
  gsap.to('.proof-metric', {
    opacity: 1,
    y: 0,
    duration: 0.6,
    delay: 2.5
  });
  
  // CTA button
  gsap.to('.fusion-cta', {
    opacity: 1,
    y: 0,
    scale: 1,
    duration: 0.6,
    ease: 'elastic.out(1, 0.6)',
    delay: 2.7
  });
  
  // Start particles
  setTimeout(()
```